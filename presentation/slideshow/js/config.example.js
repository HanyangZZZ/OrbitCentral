/* ==========================================================================
   Slideshow demo credentials (optional)
   Copy this file to config.js (gitignored) and fill in a real demo account
   to let the slideshow auto-authenticate against the live API for the
   search/bookmark demo. Without it, those calls are skipped gracefully.
   Also add <script src="js/config.js"></script> before api.js in index.html.
   ========================================================================== */

window.ORBIT_DEMO_CREDENTIALS = {
  username: 'your-demo-account@example.com',
  password: 'your-demo-account-password',
};
