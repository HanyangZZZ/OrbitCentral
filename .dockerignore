# Prevent host build artifacts from leaking into Docker build context.
# The Nginx Dockerfile runs its own npm ci + vite build inside the container.
frontend-demo/node_modules
frontend-demo/dist
frontend-demo/dist-ssr
frontend-demo/.vite
backend/__pycache__
backend/.venv
*.pyc
.DS_Store
.git
backups/
